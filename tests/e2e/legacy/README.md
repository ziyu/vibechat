# Legacy E2E Snapshot

本目录保存已从活动 VibeChat 产品路由图退出的旧 SaaS 回归，包括定价、支付、AI、affiliate 和通用 Admin。Playwright 默认 `testDir` 仍为 `tests/e2e/specs`，因此这些历史用例不会进入当前产品全量回归。

只有在对应能力完成产品范围评审并重新进入一个有 owner 的应用后，才能把相关用例迁回活动目录；不得仅为了让历史测试运行而恢复 legacy 页面。
