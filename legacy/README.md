# Legacy Source Snapshot

> 生命周期：开发中
> 状态：隔离
> 更新日期：2026-08-12

本目录保留已经退出活动构建的旧 Web/API、provider 库、用户返利/积分写入闭环、配置、测试与文档，用于迁移审计，不参与 workspace 构建或默认回归。

已重新采用的 Admin UI 位于 `apps/admin-app`，Admin API 位于 `apps/backend`；AI、支付 provider、推荐归因、佣金写入、用户提现和积分余额写入仍属未采用 legacy，不得从活动源码反向导入。
